# دليل الاختبار الشامل - محرك التحسين (Optimization Engine)

يحتوي هذا الدليل على جميع الخطوات اللازمة لاختبار الميزات المتقدمة للتطبيق لضمان عملها بشكل سليم.

## 1. إعداد بيئة الاختبار (ADB)
للحصول على كامل الصلاحيات (مثل منع إلغاء التثبيت)، يجب تعيين التطبيق كـ "صاحب الجهاز" (Device Owner).
قم بتوصيل الهاتف بالكمبيوتر وتشغيل الأمر التالي:
```bash
adb shell dpm set-device-owner com.example.parentalcontrol/.MyDeviceAdminReceiver
```

---

## 2. ميزات التخفي والوصول
- [ ] **إخفاء الأيقونة**: بمجرد تشغيل التطبيق لأول مرة، يجب أن تختفي الأيقونة من قائمة التطبيقات.
- [ ] **الرمز السري**: افتح لوحة الاتصال، واطلب `*#1234#`. يجب أن تفتح واجهة "Hidden Settings" فوراً.
- [ ] **قائمة Recent Apps**: تأكد أن التطبيق لا يظهر في قائمة "التطبيقات المفتوحة مؤخراً" عند قفل الواجهة.

## 3. التحكم عن بعد (من لوحة تحكم الويب)
- [ ] **قفل الشاشة (LOCK)**: اضغط على زر القفل في الويب. يجب أن تقفل شاشة الهاتف في أقل من ثانية.
- [ ] **الإنذار (ALARM)**: اضغط على زر الإنذار. يجب أن يصدر الهاتف صوتاً عالياً لمدة 10 ثوانٍ.
- [ ] **لقطة شاشة (CAPTURE)**: اضغط على الزر، انتظر 5 ثوانٍ، ثم تحقق من ظهور الصورة في "معرض الصور" بلوحة التحكم.

## 4. المراقبة التلقائية
- [ ] **تتبع الموقع**: اترك الهاتف لمدة 10 دقائق، ثم تحقق من تحديث الموقع على الخريطة في الويب.
- [ ] **تتبع التطبيقات**: افتح تطبيق (WhatsApp) لمدة دقيقة، ثم انتظر دورة الرفع (أو قم بتشغيلها يدوياً) وتحقق من ظهور الإحصائية في جدول "الاستخدام".
- [ ] **اعتراض الإشعارات**: أرسل رسالة تجريبية للهاتف. تحقق من ظهور محتواها في "سجل الإشعارات" بالويب.

## 5. الثبات (Persistence)
- [ ] **إعادة التشغيل**: قم بإعادة تشغيل الهاتف. انتظر دقيقة، ثم تحقق من لوحة الويب لرؤية تحديث جديد للموقع أو لقطة شاشة.
- [ ] **محاولة الحذف**: اذهب لإعدادات التطبيقات وحاول حذف "Optimization Engine". يجب أن يكون زر "إلغاء التثبيت" غير مفعّل.

---

> [!TIP]
> في حال عدم ظهور البيانات في الويب، تأكد من صحة الـ `SUPABASE_URL` والـ `SUPABASE_KEY` في ملف `dashboard/index.html`.
