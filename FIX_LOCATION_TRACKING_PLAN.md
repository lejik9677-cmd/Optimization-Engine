# خطة إصلاح مشكلة خريطة التتبع المباشر (Invalid LatLng)

تهدف هذه الخطة إلى حل مشكلة انهيار وتوقف خريطة التتبع المباشر في لوحة التحكم الناتجة عن محاولة تحريك الخريطة إلى إحداثيات غير صالحة أو قيم `NaN` عند تهيئة الخريطة أو تبديل التبويبات.

## مراجعة وتدقيق المشكلة

تظهر الأخطاء التالية في كونسول المتصفح:
1. `fetchPositions: Error: Invalid LatLng object: (NaN, NaN)` عند استدعاء `flyTo` في الملف `fleet_logic.js`.
2. فشل طلبات `OSRM Match` بطلب غير صالح (HTTP 400) نتيجة تباعد النقاط أو عدم انتظام الطوابع الزمنية، وهو ما يتم معالجته حالياً عبر مسار بديل (Fallback Route)، ولكن يجب التأكد من عدم تسبب ذلك في تمرير قيم فارغة.

المسبب الرئيسي لخطأ الـ `NaN` هو محاولة استدعاء `mapInstance.flyTo` بينما تكون أبعاد حاوية الخريطة في الـ DOM غير مستقرة أو صفرية بسبب تبديل التبويبات بشكل متزامن، أو وجود إحداثيات غير صالحة لم يتم استبعادها بشكل كامل.

## التغييرات المقترحة

### [Optimization Engine - Dashboard]

سلسلة من الإصلاحات الوقائية في ملف منطق لوحة التحكم.

#### [MODIFY] [fleet_logic.js](file:///D:/Antigravity/Optimization-Engine/dashboard/fleet_logic.js)

- **أولاً**: تحديث دالة `filterGPSPoints` لضمان تحويل القيم الصالحة إلى أرقام بشكل صريح باستخدام `parseFloat` والتأكد من استبعاد أي قيمة غير صالحة.
- **ثانياً**: تحديث دالة `fetchPositions`:
  - إضافة استدعاء لـ `mapInstance.invalidateSize()` قبل استدعاء `flyTo` للتأكد من أن Leaflet يعيد احتساب حجم الحاوية بعد تبديل التبويب.
  - إحاطة استدعاء `flyTo` بكتلة حماية `try-catch` لمنع انهيار البرنامج في حال فشل Leaflet في تحديث الموقع الرسومي.
  - التحقق من صلاحية المتغير `latest` والإحداثيات الخاصة به بشكل صريح قبل إنشاء العلامة (Marker) أو تحريك الكاميرا إليها.

## خطة التحقق والتحصيل

### التحقق اليدوي
- فتح لوحة التحكم والانتقال إلى تبويب التتبع المباشر (`track`).
- التأكد من عدم ظهور خطأ `Invalid LatLng` في كونسول المتصفح عند التنقل بين التبويبات.
- التحقق من رسم المسارات بالشكل الصحيح حتى في حال فشل OSRM Match والاعتماد على الـ Route Fallback.
